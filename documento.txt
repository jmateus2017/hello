. code
git config --global user.name ="jmateus"
git config --global user.email = "vdfdf@dfs.com"
touch hellogit.py
git init
git status
git add hellogit.py
git commit -m "Primer commit"
git log


git checkout hellogit.py (no guardar ultimo cambio)
git reset (actualiza sin cambiar el ultimo cambio)


git config --global alias.tree "log --graph --decorate --all --oneline"
git tree

touch .gitignore (crear archivo de los nombres de archivos a ignorar)
dentro de gitignore
**/<.nombredel archivo>
**/.DS_store

git add .gitignore
git commit -m "Se añade el gitignore"

Si queremos ver los cambios antes de (git add)
git diff

git reset --hard   nos lleva al punto indicado y borra todo lo que se realizo después

y para deshacer el hard

git reflog (Historial completo de commit y todo lo realizado)

 git tag clase_1 (Esto estando en el punto que queremos marcar)

 git checkout tags/clase_1 (Esto para ir a ese punto guardado )

git revert (es peligroso para eliminar un commit determinado Peligroso)


Crear una nueva rama

Git Branch login

git branch login
 git switch login

git merge master (se tre lo que esta en master a la rama login)

git stash (guardar algo pero vque no esta terminado pero debo moverme guardado temporal )

luego si me puedo cambiar

git switch master (lo que es en login puede no ser terminado aun y me deja cambiarme por el stash) 

git stach pop (recupera lo que tenia temporal)
git stash drop (no recupera nada de lo que tenia temporalment)


si todo esta bien 
git branch -d login (eliminar la rama)

crear valdacion ssh en GitHub
 ssh-keygen -t ed25519 -C "jairomateus2011@gmail.com"

eval "$(ssh-agent -s)"


/*** Agregar la clave publica en GitHub (proceso en github)
ssh -T git@github.com
si da mensaje de agregar se agrega yes
ssh -T git@github.com


git remote add origin git@github.com:jmateus2017/hello.git

git push -u origin main
git push -u origin master

Cambio desde Github rama master 









